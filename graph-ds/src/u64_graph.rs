use crate::Graph;

impl Graph<u64> {
    /// add an edge to the graph, if the nodes don't exist, they will be created
    /// TODO: implement
    pub fn random_graph(&self) -> anyhow::Result<()> {
        Ok(())
    }
}
